{
  "name": "@myrmecia/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Myrmecia command-line client — drive the orchestrator from your terminal (zero runtime dependencies).",
  "bin": {
    "myrmecia": "src/index.ts"
  },
  "scripts": {
    "start": "node src/index.ts",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22"
  }
}
